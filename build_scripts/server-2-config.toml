location = "local-2"
server_id = 2
num_clients = 1 # Hardcoded number of client connections to wait for
listen_address = "127.0.0.1"
listen_port = 8002
output_filepath = "./logs/server-2.json"

[clock]
drift_rate_us_per_sec = 500
sync_uncertainty_us = 100
sync_period_us = 10000
seed = 42
